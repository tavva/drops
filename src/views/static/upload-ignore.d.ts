// ABOUTME: Types for the hand-written upload-ignore.js module shared by the browser and server upload code.
// ABOUTME: Served as-is to the browser; the build copies it into dist/ so the compiled server can import it.
export function shouldIgnore(path: string): boolean;
