// ABOUTME: Types for the hand-written upload-ignore.js module served to the browser.
// ABOUTME: The .js file ships as-is to avoid a build step for /app/static/.
export function shouldIgnore(path: string): boolean;
